# Cloudflare PREPENDS a managed block above this file at the edge — the AI
# training-crawler blocks and the Content-Signal line come from there, not from
# here, and cannot be edited from this repo. Fetch the live URL to see the whole
# thing; reading only the top of it makes this file look like it was replaced.
# It is not: everything below is served intact.
#
# Disallow stops CRAWLING. It does not stop INDEXING, and the two get confused
# constantly. A page that is disallowed but linked from elsewhere can still be
# listed in search results — as a bare URL, because the crawler was never
# allowed to fetch the page and read the noindex telling it to stay out. So for
# anything that carries a noindex, the disallow is worse than useless: it is the
# thing preventing the noindex from being obeyed. Disallow is only right for
# content that should never be fetched at all.

User-agent: *
Allow: /

# Not deployed at all any more — stage-deploy.py keeps build sources and post
# markdown out of dist/, so these paths 404. Kept as a second line of defence:
# if the deploy is ever pointed back at the repo root, these two directories are
# the ones that leak unpublished drafts and internal material, and a crawler
# that reads this file will not touch them in the window before someone notices.
Disallow: /posts/
Disallow: /email-templates/

# Gated lead magnet. This one should genuinely never be fetched: it is the free
# sample chapter, and the whole point is that you get it by joining the list.
# Nothing links to it publicly, so there is no URL-only-indexing risk to trade
# against. It also carries X-Robots-Tag: noindex from _headers as a backstop.
Disallow: /assets/glp1-ch1-sample-7k2m9x.pdf

# DELIBERATELY NOT DISALLOWED, and each one is a correction of a rule that used
# to be here:
#
#   /blog/nature-glp1 — no longer gated at all. It was shielded during the KDP
#   review of Your Body's Own GLP-1; the title was approved, so the shield came
#   off on 2026-08-07 and it is a normal indexable article that belongs in the
#   sitemap. (The rule that used to sit here never worked anyway: it named the
#   .html form, which Pages 308-redirects away.)
#
#   /glp1-chapter-thanks and /support-kit-thanks — still not for search, but a
#   Disallow is the wrong tool. Both carry noindex in the page source and in
#   _headers, and a crawler that is not allowed to fetch them can never read
#   that noindex. Allowing the fetch is what gets them dropped from the index
#   rather than merely left uncrawled.

Sitemap: https://book.laveenaarchers.com/sitemap.xml
